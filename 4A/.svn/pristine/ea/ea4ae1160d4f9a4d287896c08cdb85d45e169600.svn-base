/* Bad Code by Katherine Kraemer. 
   There is, ideally, 1 error in this code. */
#include <string>
#include <iostream>

using namespace std;

class Pokemon{
	private:
		string name;
		int number;
		string* types;
		double height; // Meters
		double weight; // Kilograms
	public:
		void SetName(string n){ name = n; };
		string GetName(){ return name; };
		void SetNumber(int no){ number = no; };
		int GetNumber(){ return number; };
		void SetType1(string t1){ types[0] = t1; };
		string GetType1(){ return types[0]; };		
		void SetType2(string t2){ types[1] = t2; };
		string GetType2(){ return types[1]; };		
		void SetHeight(double h){ height = h; };
		double GetHeight(){ return height; };
		void SetWeight(double w){ weight = w; };
		double GetWeight(){ return weight; };
};

int main(){
	b.SetName("BULBASAUR");
	b.SetNumber(1);
	b.SetType1("GRASS");
	b.SetType2("POISON");
	b.SetHeight(0.7);
	b.SetWeight(6.9);
	
	
	cerr<<"The pokemon ";
	cout<<b.GetName()<<" is #"<<b.GetNumber()<<" in the National Pokedex. Its type is "<<b.GetType1()<<b.GetType2()<<"."<<endl;

}
