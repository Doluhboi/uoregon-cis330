// This program is from: Sam Deitle
// The author believes it has <X> memory errors


#include <stdio.h>

int main()
{
	char *f;
	char *buffer;
	
	int it_count;

	cin>>f;
	FILE *file = fopen(f, "rw");
	fseek(file, 0, SEEK_END);
	it_count  = ftell(file);
	fseek(file, 0, SEEK_SET);
	
	int i;
	for (i = 0; i < it_count; i++)
	{
		fread(file, 
	}		

}
